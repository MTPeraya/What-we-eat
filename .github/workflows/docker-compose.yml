name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        ports: ["5432:5432"]
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: whatweeat_test

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Backend tests
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/whatweeat_test
        run: |
          cd backend
          npm ci
          npm test

      - name: Frontend tests
        run: |
          cd frontend
          npm ci
          npm test

  build-and-push:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v3
      - uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build & Push Backend
        run: |
          docker build -t mperaya/wwe-backend:${{ github.sha }} -t mperaya/wwe-backend:latest ./backend
          docker push mperaya/wwe-backend:${{ github.sha }}
          docker push mperaya/wwe-backend:latest

      - name: Build & Push Frontend
        run: |
          docker build -t mperaya/wwe-frontend:${{ github.sha }} -t mperaya/wwe-frontend:latest ./frontend
          docker push mperaya/wwe-frontend:${{ github.sha }}
          docker push mperaya/wwe-frontend:latest

=======
name: Docker Compose CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 🛒 Checkout repository
        uses: actions/checkout@v3

      - name: 🟢 Use Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # Backend tests
      - name: 📦 Install backend dependencies
        run: |
          cd backend
          npm install

      - name: 🧪 Run backend tests
        run: |
          cd backend
          npm test

      # Frontend tests
      - name: 📦 Install frontend dependencies
        run: |
          cd frontend
          npm install

      - name: 🧪 Run frontend tests
        run: |
          cd frontend
          npm test

  build-and-push:
    runs-on: ubuntu-latest
    needs: test   # only run if tests pass

    steps:
      - name: 🛒 Checkout repository
        uses: actions/checkout@v3

      - name: 🔑 Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: mperaya
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: 🛠 Build and push Docker images
        run: |
          docker compose -f docker-compose.yml build
          docker compose -f docker-compose.yml push
