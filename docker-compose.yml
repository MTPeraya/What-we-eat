version: "3.8"
services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    container_name: react-frontend

  backend:
    build:
      context: ./backend
    ports:
      - "4001:3000"
    container_name: next-backend
